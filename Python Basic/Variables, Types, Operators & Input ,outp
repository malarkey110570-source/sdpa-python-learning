---
course: EMATM0048
module: Software Development - Programming and Algorithms
week: 1
topic: Python Basics
date: 2026-09-22
tags:
  - Python
  - SDPA
  - EMATM0048
  - Bristol
  - Tutorial1
---

# Week 01 - Python Basics

> [!summary]
> 本周主要学习 Python 最基础的组成部分：
> **变量、数据类型、输入输出、类型转换、字符串、运算符、运算优先级，以及简单的 `if / else`。**
>
> 最大的收获不是记住语法，而是开始学习：
>
> **先把问题拆成小步骤，再把每一步翻译成 Python。**

---

# 1. Variables | 变量

Python 使用 `=` 给变量赋值。

```python
x = 1
y = x + 5
z = y

print(x)
print(y)
print(z)
```

输出：

```text
1
6
6
```

## `=` 的含义

```python
x = 5
```

意思是：

> 把右边的值 `5` 赋给变量 `x`。

不是数学里的“等式判断”。

---

# 2. Basic Data Types | 基本数据类型

本周主要接触四种类型：

| 类型 | 英文 | 示例 |
|---|---|---|
| `int` | Integer | `5` |
| `float` | Floating Point | `5.0` |
| `str` | String | `"5"` |
| `bool` | Boolean | `True` |

例如：

```python
x = 1
print(type(x))
```

输出：

```text
<class 'int'>
```

而：

```python
x = 1.0
print(type(x))
```

输出：

```text
<class 'float'>
```

> [!important]
> `1` 和 `1.0` 数值上相等，但 Python 会把它们作为不同的数据类型处理。

---

## Boolean

```python
x = True
print(type(x))

x = False
print(type(x))
```

输出类型：

```text
<class 'bool'>
```

Boolean 主要用于：

- 条件判断
- 程序分支
- `if / else`

---

# 3. `input()` | 用户输入

Python 可以通过：

```python
input()
```

读取键盘输入。

例如：

```python
name = input("Enter your name: ")
print(name)
```

---

## `input()` 永远返回 `str`

这是本周非常重要的知识点。

```python
x = input("Enter a number: ")

print(x)
print(type(x))
```

即使输入：

```text
5
```

Python 仍然认为：

```text
"5"
```

是：

```text
str
```

> [!important]
> `input()` 获取的数据默认都是字符串。

---

# 4. Type Conversion | 类型转换

## `str → int`

```python
x = input("Enter a number: ")
x = int(x)
```

也可以直接：

```python
x = int(input("Enter a number: "))
```

---

## `str → float`

例如 BMI 中，身高可能是：

```text
1.79
```

所以不能只使用 `int()`。

```python
height = float(input("Enter your height: "))
```

---

## `int / float → str`

```python
age = 22

print("I am " + str(age) + " years old.")
```

因为不能直接：

```python
"string" + integer
```

---

# 5. String | 字符串

## 字符串拼接

```python
first_name = "Yang"
last_name = "Zhang"

print(last_name + " " + first_name)
```

结果：

```text
Zhang Yang
```

---

## 字符串和数字的 `+` 不一样

```python
"10" + "5"
```

得到：

```text
"105"
```

因为这是字符串拼接。

而：

```python
10 + 5
```

得到：

```text
15
```

因为这是数学加法。

---

## 字符串重复

本周在 `n + nn + nnn` 中使用过：

```python
n = "5"

nn = n * 2
nnn = n * 3
```

结果：

```text
nn = "55"
nnn = "555"
```

所以字符串的：

```python
"5" * 3
```

不是数学乘法，而是：

```text
"555"
```

---

# 6. Arithmetic Operators | 算术运算符

| 运算符 | 含义 |
|---|---|
| `+` | 加法 |
| `-` | 减法 |
| `*` | 乘法 |
| `/` | 普通除法 |
| `//` | Floor Division |
| `%` | Modulo / 取余 |
| `**` | 幂 |

---

# 7. `/`、`//`、`%`

这是本周最重要的一组运算符之一。

```python
print(17 / 3)
print(17 // 3)
print(17 % 3)
```

结果：

```text
5.666666666666667
5
2
```

---

## `/` 普通除法

```python
10 / 2
```

结果：

```text
5.0
```

即使数学结果是整数，Python 的 `/` 仍会返回 `float`。

---

## `//` Floor Division

```python
17 // 3
```

结果：

```text
5
```

表示完整的商。

---

## `%` Modulo

```python
17 % 3
```

结果：

```text
2
```

因为：

```text
17 = 3 × 5 + 2
```

---

## 商和余数可以还原原数

```python
number = 17
divisor = 3

original = (number // divisor) * divisor + (number % divisor)

print(original)
```

结果：

```text
17
```

公式：

```text
原数 = 商 × 除数 + 余数
```

---

# 8. Operator Precedence | 运算优先级

目前需要记住：

```text
()
↓
* / // %
↓
+ -
```

例如：

```python
2 + 3 * 4
```

先算：

```text
3 * 4 = 12
```

再：

```text
2 + 12 = 14
```

---

如果有括号：

```python
(2 + 3) * 4
```

先算：

```text
2 + 3 = 5
```

再：

```text
5 * 4 = 20
```

---

## 同一级运算符

例如：

```python
20 / 5 * 2
```

`/` 和 `*` 优先级相同，因此从左往右：

```text
20 / 5 = 4.0

4.0 * 2 = 8.0
```

> [!warning]
> 运算优先级是目前需要继续加强的知识点。

---

# 9. Comparison Operators | 比较运算符

本周实际主要使用：

```python
==
```

表示：

> 是否相等？

例如：

```python
number % 2 == 0
```

---

## `=` 和 `==`

```python
x = 5
```

表示：

```text
赋值
```

而：

```python
x == 5
```

表示：

```text
判断 x 是否等于 5
```

> [!warning]
> `=` 和 `==` 不能混用。

---

# 10. `if / else`

本周在奇偶判断题第一次实际使用。

基本格式：

```python
if condition:
    code
else:
    code
```

例如：

```python
number = int(input("Please enter an integer: "))

if number % 2 == 0:
    print("Even")
else:
    print("Odd")
```

逻辑：

```text
number % 2 == 0
        ↓
      True
        ↓
      Even
```

否则：

```text
Odd
```

---

## 缩进

Python 使用缩进表示代码属于哪个代码块。

```python
if number % 2 == 0:
    print("Even")
```

通常使用：

```text
4 spaces
```

---

# 11. Exercise 1 - Square and Cube

目标：

> 输入一个数字，计算平方和立方。

我的练习：

```python
number = int(input("Enter a number: "))

x = number * number
y = number * number * number

print(x)
print(y)
```

更清晰的变量名：

```python
number = int(input("Enter a number: "))

squared = number * number
cubic = number * number * number

print(squared)
print(cubic)
```

---

# 12. Exercise 2 - BMI

输入：

```python
height = float(input("Enter your height: "))
weight = float(input("Enter your weight: "))
```

正确公式：

```python
bmi = weight / (height * height)
```

输出：

```python
print("Your BMI is " + str(bmi))
```

---

## 我最初的错误

最开始写成：

```python
bmi = weight / height * height
```

Python 会按照：

```text
(weight / height) * height
```

计算。

最后基本又得到：

```text
weight
```

正确写法：

```python
weight / (height * height)
```

> [!important]
> 括号不只是为了代码好看，它可以改变计算顺序。

---

# 13. Exercise 3 - `(x + y) * (x + y)`

```python
x = int(input("Please enter an integer: "))
y = int(input("Please enter an integer: "))

result = (x + y) * (x + y)

print("Your result is " + str(result))
```

这里主要练习：

- `input()`
- `int()`
- 运算符
- 括号
- `str()`

---

# 14. Exercise 4 - 12-Hour Clock

题目：

```text
8:00 am
+
9 hours
```

数学：

```text
8 + 9 = 17
```

但 12 小时制中：

```text
17 % 12 = 5
```

代码：

```python
start_time = 8
hours_later = 9

final_time = (start_time + hours_later) % 12

print(str(final_time) + " pm")
```

得到：

```text
5 pm
```

这里第一次理解 `%` 可以处理：

> 周期 / 循环问题

不仅仅是求数学余数。

---

# 15. Input and Output Exercise

## Welcome Message

```python
name = input("Please enter your name: ")

print("Hello " + name + ", would you like to learn some Python today?")
```

---

## 为什么不用 `str(input())`

错误但可以运行：

```python
name = str(input("Please enter your name: "))
```

因为：

```python
input()
```

本身已经返回：

```text
str
```

所以外面的 `str()` 是多余的。

---

# 16. First Name + Last Name

```python
first_name = input("Please enter your first name: ")
last_name = input("Please enter your last name: ")

print("Welcome " + last_name + " " + first_name + "!")
```

这里练习了：

```text
字符串拼接
```

以及：

```python
" "
```

一个只包含空格的字符串。

---

# 17. Challenge - `n + nn + nnn`

例如：

```text
n = 5

5 + 55 + 555 = 615
```

我的思路：

> 先把输入保留为字符串，利用字符串重复生成 `55` 和 `555`，最后再转回整数进行计算。

```python
n = input("Please enter an integer: ")

nn = n * 2
nnn = n * 3

result = int(n) + int(nn) + int(nnn)

print(result)
```

例如：

```text
n = "5"
nn = "55"
nnn = "555"
```

然后：

```text
5 + 55 + 555 = 615
```

---

# 18. Challenge - Even or Odd

```python
number = int(input("Please enter an integer: "))

if number % 2 == 0:
    print("Even")
else:
    print("Odd")
```

原理：

```text
偶数 % 2 = 0
```

例如：

```text
8 % 2 = 0
```

---

## 本题出现的错误

最开始写成：

```python
if number % 2 = 0
```

错误原因：

```text
= 是赋值
== 才是比较
```

还需要：

```python
else:
```

不能漏掉：

```text
:
```

---

# 19. Challenge - 四位数各位求和

目标：

```text
1234
↓
1 + 2 + 3 + 4
↓
10
```

核心技巧：

```text
% 10
```

取最后一位。

```text
// 10
```

去掉最后一位。

---

## 例子

```text
1234 % 10 = 4

1234 // 10 = 123
```

继续：

```text
123 % 10 = 3
123 // 10 = 12
```

---

## 最终代码

```python
number = int(input("Enter a four-digit integer: "))

digit1 = number % 10
number = number // 10

digit2 = number % 10
number = number // 10

digit3 = number % 10
number = number // 10

digit4 = number % 10

result = digit1 + digit2 + digit3 + digit4

print(result)
```

> [!note]
> 当前变量名中：
>
> `digit1` 实际是个位  
> `digit2` 是十位  
> `digit3` 是百位  
> `digit4` 是千位

---

# 20. Challenge - Running Time

题目：

```text
Start: 6:52 am

1 mile @ 8:15
3 miles @ 7:12
1 mile @ 8:15
```

---

## Step 1 - 全部转换成秒

Easy pace：

```text
8 × 60 + 15
= 495 seconds
```

Tempo pace：

```text
7 × 60 + 12
= 432 seconds
```

三英里：

```text
432 × 3
= 1296 seconds
```

总时间：

```text
495 + 1296 + 495
= 2286 seconds
```

---

## Step 2 - 秒转换成分钟

```text
2286 // 60 = 38
2286 % 60 = 6
```

所以：

```text
38 minutes 6 seconds
```

---

## Step 3 - 加到开始时间

```text
6:52
+
0:38
```

分钟：

```text
52 + 38 = 90
```

然后：

```text
90 // 60 = 1
```

进位一个小时。

```text
90 % 60 = 30
```

剩余 30 分钟。

最终：

```text
7:30:06 am
```

---

## 最终代码

```python
easy_pace_time = 8 * 60 + 15
tempo_pace_time = (7 * 60 + 12) * 3

total_time = easy_pace_time * 2 + tempo_pace_time

cost_minute = total_time // 60
final_second = total_time % 60

start_hour = 6
start_minute = 52

total_minute = start_minute + cost_minute

final_hour = start_hour + total_minute // 60
final_minute = total_minute % 60

print(
    str(final_hour)
    + ":"
    + str(final_minute)
    + ":"
    + str(final_second)
    + " am"
)
```

---

# 21. Debugging Notes | 本周错误记录

## 中文括号

遇到：

```text
invalid character '（' (U+FF08)
```

原因：

```python
print（x）
```

用了中文全角括号。

正确：

```python
print(x)
```

> [!warning]
> 写 Python 时尽量切换到英文输入法。

---

## `str + int`

错误：

```python
x = 5

print("The number is " + x)
```

原因：

```text
str + int
```

不能直接拼接。

正确：

```python
print("The number is " + str(x))
```

---

## `=` vs `==`

```python
x = 5
```

是赋值。

```python
x == 5
```

是比较。

---

## 忘记冒号

错误：

```python
else
```

正确：

```python
else:
```

---

## 运算符空格

推荐：

```python
x = y + 5
```

而不是：

```python
x=y+5
```

---

# 22. PEP 8 Basic Style | 基础代码规范

目前需要保持的习惯：

## 运算符两侧留空格

```python
bmi = weight / (height * height)
```

---

## 函数和括号之间不要加空格

推荐：

```python
print(x)
int(x)
```

不推荐：

```python
print (x)
int (x)
```

---

## 逗号后加空格

```python
print(x, y, z)
```

---

## 注释

```python
# Calculate BMI
```

`#` 后面留一个空格。

---

## 变量命名

优先使用：

```python
start_hour
final_minute
easy_pace_time
```

而不是大量使用：

```python
x
y
z
```

如果变量代表具体含义，最好让名字体现这个含义。

---

# 23. Spyder Notes

## Undo

```text
Ctrl + Z
```

Redo：

```text
Ctrl + Shift + Z
```

部分情况下：

```text
Ctrl + Y
```

---

## 为什么明明框选代码却出现 `Enter your name`

原因是之前的程序仍停在：

```python
input()
```

等待用户输入。

或者运行了：

```text
Run File
```

导致 Spyder 从文件开头执行整个程序。

`input()` 会暂停程序，直到用户输入内容并按 Enter。

---

# 24. 本周小测结果

## 已经掌握

能够判断：

```text
10      → int
10.0    → float
"10"    → str
False   → bool
```

能够判断：

```python
x = "8"
x + x
```

结果：

```text
88
```

而：

```python
y = 8
y + y
```

结果：

```text
16
```

---

能够计算：

```text
23 / 5  = 4.6
23 // 5 = 4
23 % 5  = 3
```

---

能够独立发现以下错误：

```text
input() 返回 str
= 和 == 不同
else 后需要 :
```

---

# 25. 当前薄弱点

> [!warning]
> 当前最需要加强的是：
>
> **Operator Precedence | 运算优先级**

曾经错误判断：

```python
(2 + 3) * 4
```

需要注意：

```text
(2 + 3) = 5
5 × 4 = 20
```

而：

```python
20 / 5 * 2
```

同级运算从左向右：

```text
20 / 5 = 4.0
4.0 × 2 = 8.0
```

---

# 26. 本周核心思维

这一周开始理解：

```text
Programming Problem
        ↓
拆成小问题
        ↓
确定输入
        ↓
确定变量
        ↓
选择运算
        ↓
一步一步计算
        ↓
输出结果
```

例如四位数拆位：

```text
1234
↓
% 10
↓
4

1234
↓
// 10
↓
123
```

再重复。

---

# 27. 本周我已经能够做到

- [x] 创建变量
- [x] 使用 `int`
- [x] 使用 `float`
- [x] 使用 `str`
- [x] 使用 `bool`
- [x] 使用 `type()`
- [x] 使用 `input()`
- [x] 使用 `print()`
- [x] 使用 `int()`
- [x] 使用 `float()`
- [x] 使用 `str()`
- [x] 字符串拼接
- [x] 字符串重复
- [x] 使用 `/`
- [x] 使用 `//`
- [x] 使用 `%`
- [x] 基础运算优先级
- [x] 基础 `if / else`
- [x] 判断奇偶数
- [x] 使用 `% 10` 拆数字
- [x] 使用 `// 10` 删除最后一位
- [x] 使用 `// 60` 和 `% 60` 做时间单位转换
- [x] 阅读简单 Python 报错
- [x] 开始使用 PEP 8 风格

---

# 28. 下次复习 Checklist

- [ ] 再练习 5 道运算优先级题
- [ ] 熟练区分 `/`、`//`、`%`
- [ ] 不再混淆 `=` 和 `==`
- [ ] 熟练写 `if / else`
- [ ] 记住 4 空格缩进
- [ ] 继续练习类型转换
- [ ] 尽量使用有意义的变量名
- [ ] 写代码前先想步骤，而不是马上开始敲
- [ ] 继续保持英文符号输入
- [ ] 独立完成简单题后再查看提示

---

# Week 01 Summary

本周从完全基础的：

```python
x = 1
```

一路练习到了：

```text
变量
↓
数据类型
↓
input/output
↓
类型转换
↓
字符串
↓
数学运算
↓
/ // %
↓
运算优先级
↓
if / else
↓
数字拆位
↓
简单算法问题
```

目前已经能够阅读和编写简单的 Python 程序。

下一步重点不是急着学习更多语法，而是继续提高：

> **运算理解 + 问题拆解 + 独立写代码的能力。**
